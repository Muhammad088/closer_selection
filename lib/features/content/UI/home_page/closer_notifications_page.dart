import 'package:flutter/material.dart';

class CloserNotificationsPage extends StatelessWidget {
  const CloserNotificationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("closer notifications Page"),
      ),
      body: const Center(
        child: Text("closer notifications Page"),
      ),
    );
  }
}
