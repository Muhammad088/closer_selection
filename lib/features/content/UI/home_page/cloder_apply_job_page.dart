import 'package:flutter/material.dart';

class CloserApplyJobPage extends StatelessWidget {
  const CloserApplyJobPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Closer ApplyJob Page"),
      ),
      body: const Center(
        child: Text("Closer ApplyJob Page"),
      ),
    );
  }
}
