import 'package:flutter/material.dart';

class CoachCreateJobPage extends StatelessWidget {
  const CoachCreateJobPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach CreateJob Page"),
      ),
      body: const Center(
        child: Text("Coach CreateJob Page"),
      ),
    );
  }
}
