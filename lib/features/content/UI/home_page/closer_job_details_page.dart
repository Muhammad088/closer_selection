import 'package:flutter/material.dart';

class CloserJobDetailsPage extends StatelessWidget {
  const CloserJobDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Closer Job Details Page"),
      ),
      body: const Center(
        child: Text("Closer Job Details Page"),
      ),
    );
  }
}
