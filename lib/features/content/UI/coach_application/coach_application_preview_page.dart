import 'package:flutter/material.dart';

class CoachApplicationPreviewPage extends StatelessWidget {
  const CoachApplicationPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach ApplicationPreview Page"),
      ),
      body: const Center(
        child: Text("Coach ApplicationPreview Page"),
      ),
    );
  }
}
