import 'package:flutter/material.dart';

class CoachApplicationListPage extends StatelessWidget {
  const CoachApplicationListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach ApplicationList Page"),
      ),
      body: const Center(
        child: Text("Coach ApplicationList Page"),
      ),
    );
  }
}
