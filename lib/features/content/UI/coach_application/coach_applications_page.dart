import 'package:flutter/material.dart';

class CoachApplicationsPage extends StatelessWidget {
  const CoachApplicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach Applications Page"),
      ),
      body: const Center(
        child: Text("Coach Applications Page"),
      ),
    );
  }
}
