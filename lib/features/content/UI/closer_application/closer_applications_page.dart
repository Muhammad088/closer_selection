import 'package:flutter/material.dart';

class CloserApplicationsPage extends StatelessWidget {
  const CloserApplicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("closer Applications Page"),
      ),
      body: const Center(
        child: Text("closer Applications Page"),
      ),
    );
  }
}
