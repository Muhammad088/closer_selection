import 'package:flutter/material.dart';

class CloserSetAlarmPage extends StatelessWidget {
  const CloserSetAlarmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Closer SetAlarm Page"),
      ),
      body: const Center(
        child: Text("Closer SetAlarm Page"),
      ),
    );
  }
}
