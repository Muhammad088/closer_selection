import 'package:flutter/material.dart';

class CloserApplicationPreviewPage extends StatelessWidget {
  const CloserApplicationPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("closer ApplicationPreview Page"),
      ),
      body: const Center(
        child: Text("closer ApplicationPreview Page"),
      ),
    );
  }
}
