import 'package:flutter/material.dart';

class CoachSettingPage extends StatelessWidget {
  const CoachSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach profile setting"),
      ),
      body: const Center(
        child: Text("Coach profile setting"),
      ),
    );
  }
}
