import 'package:flutter/material.dart';

class CoachEditProfilePage extends StatelessWidget {
  const CoachEditProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Coach Edit Profile Page"),
      ),
      body: const Center(
        child: Text("Coach Edit Profile Page"),
      ),
    );
  }
}
